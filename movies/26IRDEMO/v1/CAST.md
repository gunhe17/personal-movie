# 배역과 줄거리 — 아홉 장면을 관통하는 것

장면은 따로 찍지만 한 편으로 붙는다. **누가 나오는지가 장면마다 바뀌면 관객은 한 센터의 하루로 읽지 않는다.**
이 문서가 그 정본이다. 장면 준비(`scene-prep`)는 여기서 배역을 가져다 쓰고, 어긋나면 여기를 먼저 고친다.

## 두 제품, 한 사람들

**s01이 만드는 인물은 마인드봄 시드에도 있다.** 두 제품은 연동되지 않으므로(§함정) 접수해도 저쪽에 생기지 않는다.
그래서 `_mocks/s01-intake.json`의 명단이 **배역의 두 번째 원천**이고, 마인드봄 `cast.py`가 이름·생년월일을 그대로 따른다.
`_scripts/check-cast.py`가 시드와 목 대본 양쪽에 대조한다 — 어느 쪽에도 없는 사람이 마인드봄에 있으면 깨진다.

한 영상이 두 제품을 오간다 — **마인드스코프**(접수 · 관리 · 바로링크 · 회기)에서 접수한 아이가
**마인드봄**(투사검사 실시 · 채점 · 해석 보조)에서 검사받는다.

배역의 정본은 마인드스코프 develop 시드 하나다(`_tool/saas-center-platform/apps/api/scripts/seed/develop`).
마인드봄의 `apps/api/scripts/cast.py`가 그것을 따라가고, 어긋나면 `_scripts/check-cast.py`가 깨진다.
**시드를 바꾸지 않는다** — 없는 상태는 장면 안에서 만들거나 촬영 전 준비 단계에서 만든다.

## 주인공은 상담사 한 명

**정상담(counselor1)** — 전 장면 고정. 마인드스코프에서는 COUNSELOR, 마인드봄에서는 clinician이고
마인드봄의 `CLINICIAN_KEY`도 이 사람이다(clinician은 본인이 examiner인 검사만 보므로 어긋나면 화면이 빈다).
로그인 상태는 `_state/local-saas-counselor1.json`.

조사가 말한 "식별 가능한 피해자"는 상담사다(75초 마스터: 주인공 상담사 1명, 내담자는 조연).
그래서 내담자는 장면군마다 달라도 되지만, **한 축 안에서는 같은 사람이어야 한다.**

## 세 축

| 축 | 장면 | 인물 | 왜 |
|---|---|---|---|
| **검사** | s01 → s02 → **s03(마인드봄 종합보고서)** → s04 → **s06 → s07** | **햇살지역아동센터 · 윤도현(2014-05-08) · 장서아(2015-11-21) · 홍시우(2016-02-13)** — 만 10~12세, 지어낸 이름 | s01이 이 셋을 접수하고 **윤도현**이 s02(마인드봄 로샤 실시) · s03(마인드봄 종합보고서 배터리) · s04(바로링크) · **s06(필드노트) · s07(자동일지)** 를 끌고 간다. s06·s07은 보고서 제언 ①에서 이어지는 **개인상담 C00003 1회기**를 앞뒤로 쓴다 — 녹음·전사(s06)와 그 전사로 쓰이는 일지(s07)라 갈라둘 수 없다. **검사 축의 검사는 한 사람에게 모인다** — 마인드봄 배터리(로샤·HTP·SCT)가 박지우에서 윤도현으로 옮겨졌다(`_tool/mindbom/apps/api/scripts/seed.py:50-52,86-89` `battery-yundohyun`). 시드 인물로는 s01("단체 유입이면 한 명씩 등록한다"의 역상)이 성립하지 않으므로 **미등록 인물이어야 한다** |
| ~~보고서~~ (폐지) | ~~s03 테이크 A·B~~ | ~~박지우~~ | **2026-09-10 폐지.** s03은 검사 축으로 흡수됐다 — 선택본이 마인드봄 편집기의 `aireview` 하나가 되면서 주인공이 **윤도현**이다. 마인드봄 SCT가 아동·청소년 문항이라 만 35세 박지우에게 애초에 어긋나 있었고, `preset-body.ts` 본문도 아동용으로 다시 썼다. 마인드스코프 보고서 화면(`draft`)은 이번 편에서 찍지 않는다 |
| **회기** | s05 · ~~s06~~ · ~~s07~~ · s09 | **이하준**(2016-09-22, 만 9세)과 어머니 **이수진** | 시드가 이 축만 완비했다 — 놀이치료 C00002(3회 완료) · 회기별 일지 · 필드노트 연결 · 바우처 1건. **2026-09-10: s06·s07이 이 축에서 빠져 검사 축(윤도현)으로 갔다** — 아래 각주. 남은 것은 s05(보호자 이수진의 일정 변경 요청)와 s09(바우처 차감)다 |
| **모임** | s08 | **이하준** — 회기 축과 같은 사람 | 처음엔 김민준(보호자 둘 + 형제)이었으나 **주인공이 그를 못 연다**: COUNSELOR는 `access_level=own`이라 담당 내담자만 열람되고, URL 직접 접근도 같은 기준으로 막힌다(`get_client.py:14-19`). 정상담이 담당인 이하준으로 바꿨다 — 그리고 이쪽이 더 낫다. s05·s06·s07에서 **우리가 방금 쌓은 기록**이 그대로 모인 화면이 된다 |

> **s06·s07 이동 (2026-09-10) — 완료.** 사용자 요구로 s06(필드노트)의 아이를 이하준 → **윤도현**으로 옮겼고, **s07(자동일지)도 같이 옮겼다.**
> 윤도현에겐 상담 케이스가 없어 `_scripts/s06-setup.sql`이 개인상담 **C00003** 1회기(2026-09-09 16:00–16:50)를 만든다(종합보고서 제언 ①에서 이어지는 케이스).
> s07은 그 회기의 필드노트를 그대로 받는다 — **"방금 녹음한 그 회기"의 일지가 다른 아이 이름으로 나오지 않게** 한 쌍으로 묶었다.
> `s07-setup.sql`의 초안 본문은 윤도현 전사에서 다시 썼고, s06-setup.sql의 임시 §4(이하준 전사)는 지웠다.
> **s05·s08·s09는 이하준 그대로다** — 필드노트를 경유하지 않는다. s05는 보호자(이수진)가 축이고 윤도현에겐 보호자가 없다,
> s08은 담당 범위(access_level=own)가 축이라 담당 내담자여야 하며, s09는 바우처 차감이 축이고 시드가 이하준으로 완비돼 있다.

축이 갈리는 것은 타협이 아니다 — 실제 센터도 검사 케이스와 상담 케이스는 다른 사람인 경우가 많다.
지켜야 할 것은 **축 안에서 사람이 바뀌지 않는 것**이다.

## 검사 축의 나이가 정해진 이유

s01의 세 아이 나이는 임의가 아니다. 뒤 장면 둘이 나이를 강제한다.

| 강제하는 것 | 범위 | 근거 |
|---|---|---|
| **로르샤흐** — s02에서 마인드봄이 실시·채점 | 만 5세 이상 | 마인드봄이 실제로 다루는 검사는 HTP · 로르샤흐 · SCT 셋뿐이고 나머지는 `comingSoon` |
| **스마트폰중독검사** — s03 자동 채점 · s04 바로링크 | **만 10-19세** | `assessments.supports_online` — 18종 중 온라인 응시가 되는 것은 이것과 스마트폰 과의존 성인·고령층 척도(만 20세 이상) **둘뿐** |

만 10~12세는 그 교집합이다. 더 어리면 s03·s04가 불가능하고, 성인이면 아동센터 단체 접수라는 결이 깨진다.

## s01 접수가 네 검사를 함께 잡는 이유

한 번의 단체 접수에서 **네 건**을 고른다. 그러면 한 케이스가 두 갈래로 갈라진다.

```
s01 접수 (네 검사) ─┬─ 로르샤흐(투사·대면)       → s02 마인드봄에서 실시·채점 ┐
                    ├─ 집-나무-사람 HTP(투사·대면) ──────────────────────────┼→ s03 마인드봄 종합보고서
                    ├─ 문장완성검사 SCT(투사·대면) ──────────────────────────┘   (같은 battery_id로 묶인 확정 검사 2건 이상)
                    └─ 스마트폰중독검사(온라인)   → s04 바로링크로 집에서 응시 → 자동 채점
```

대면 셋은 마인드봄이 실제로 다루는 세 검사 그대로다(나머지 15종은 `comingSoon`).
온라인 한 건은 뺄 수 없다 — s04의 `바로링크 전송` 버튼이 케이스의 온라인 검사에 달려 있고,
카탈로그 18종 중 `supports_online=true`는 둘뿐인데 만 10~19세는 스마트폰중독검사 하나다.

이것은 scenes9 §02가 약속한 **"검사 유형 분리 — 자가보고식(온라인) · 투사 · 외부검사"** 그 자체다.
장면 하나를 위해 만든 우회가 아니라, 제품이 실제로 나눠 놓은 갈래를 그대로 따라간 것이다.

## 시드 조합 (2026-09-10c — 확정)

스냅샷은 `_seed/saas-2026-09-10c.json`(sha256 앞 8자리 **`c901b092`**) · `_seed/mindbom-2026-09-10c.json` 한 쌍이다.
(09-10b는 그 전 조합이다 — 이미 찍힌 테이크가 그 위에서 나왔으므로 STATUS.md의 촬영 기록에는 그대로 남는다.)
`reset-seed.sh`가 되돌리는 지점이 여기고, 그 위에 장면별 선행 SQL이 얹힌다 —
`s05-schedules.sql`(일간·주간·월간에 뜰 일정 35건) · `s05-setup.sql` · `s06-setup.sql` · `s09-setup.sql`.

**검사 축 세 아이는 양쪽 제품에 미리 있다.** 마인드스코프 시드에는 없고(s01이 화면에서 만든다),
마인드봄 `cast.py`에는 윤도현(2014-05-08) · 장서아(2015-11-21) · 홍시우(2016-02-13)로 들어 있으며
장서아·홍시우의 로르샤흐가 `created`로 대기한다. **윤도현의 로르샤흐는 배터리에 들어가면서 시드 기본이 `confirmed`가 됐다**
(`seed.py:48` · `seed_content.py:91` — 반응 22까지 채운다). s02는 `_scripts/s02-reset.sh`로 그것을 반응 0 · `created`로 되돌려 이어받고,
윤도현의 HTP·SCT(`battery-htp`·`battery-sct`)가 `confirmed`로 함께 있어 종합보고서 버튼이 열린다.
**s02와 s03은 같은 검사를 반대 상태로 쓴다** — s03 촬영 전에는 시드를 다시 돌려 로샤를 확정 상태로 되돌린다.
`_scripts/check-cast.py`는 시드와 `_mocks/s01-intake.json` 양쪽을 배역 원천으로 인정한다.

### 마인드스코프 시드 인물

| 인물 | 생년월일 | 역할 | 상담 | 검사 | 바우처 | 관계 |
|---|---|---|---|---|---|---|
| 이수진 | 1985-04-18 | guardian | | | | 이하준 모 (한부모) |
| 김철수 | 1988-12-03 | guardian | | | | 김민준·김서연 부 |
| 김영희 | 1990-08-25 | both | | AC0001 3회기 | | 김민준·김서연 모, 본인도 상담 |
| 박지우 | 1991-02-14 | client | C00001 4회 | | | 성인 — 어느 축에도 넣지 않았다(아동센터의 하루라는 결에서 벗어난다). 회기 축 대역 |
| 이하준 | 2016-09-22 | client | **C00002** 3회 | | **1** | 회기 축 |
| 김민준 | 2019-03-10 | client | | | **1** | 모임 축 |
| 김서연 | 2021-06-15 | client | | | | 김민준 여동생 |

## 장면이 강제하는 조건 · 준비 상태

| 장면 | 강제 조건 | 상태 |
|---|---|---|
| s01 | 미등록 인물 명단 · **검사 4건**(로르샤흐 · HTP · SCT · 스마트폰중독검사) | 시드 그대로. 근거는 `s01-접수/features.md` §검사 항목 4건 |
| s02 | 마인드봄에 윤도현의 로르샤흐가 **반응 0 · `created`** 로 있어야 한다 | **시드 기본은 `confirmed`+반응 22다**(배터리의 일원). `_scripts/s02-reset.sh`가 되돌린다 — 매 리허설·테이크 전에 |
| s03 | **마인드봄 배터리 `battery-yundohyun`**(윤도현의 로샤·HTP·SCT가 전부 `confirmed`) + 종합보고서 1건 | `seed.py` + `seed_content.py`. **s02가 `s02-reset.sh`로 로샤를 비워 두면 보고서가 빈다 — 그때만 재시드** |
| s04 | 케이스에 온라인 검사가 있어야 바로링크 버튼이 뜬다(`AssessmentCaseCard.svelte:117` `hasOnlineLink`) | s01을 먼저 돌린다 |
| s05 | 일정 변동 요청 1건 + 일간·주간·월간에 뜰 일정 | `s05-setup.sql` · `s05-schedules.sql`(35건) |
| s06 | **윤도현의** 상담 케이스 + 회기 + 전사 세그먼트 — 시드에 없다 | `s06-setup.sql`이 통째로 만든다. **s01을 먼저 돌린다**(윤도현이 s01의 산물). 회기는 **어제(2026-09-09) 16:00** — 오늘로 두면 s07이 안 열린다 |
| s07 | s06이 남긴 필드노트(**윤도현 C00003**) · 그 회기의 일지가 비어 있을 것 · 회기 시작 시각이 **과거**일 것 | `s06-setup.sql` → `s07-setup.sql` 순서로 돌린다. 후자가 일지를 비우고 `note_status`를 되돌리고 `production_ai_configs`로 초안 본문을 고정한다 |
| s08 | **담당 내담자여야 한다**(access_level=own) · 케이스 · 바우처 · 회기 일지 | 이하준 — 시드 그대로 |
| s09 | 회기 + 바우처 차감 | `s09-setup.sql` |

장면별 진행 상태(리허설 통과 여부 · 테이크)는 여기가 아니라 `STATUS.md`가 정본이다.
이 표는 **무엇이 있어야 그 장면이 성립하는가**만 적는다.

## 바꿀 때

축을 바꾸면 그 축의 장면 전부를 다시 본다. 이 문서를 고친 뒤 해당 장면의 `notes.md` 시드 줄과
`features.md` 배역, 목 대본의 값까지 같이 고친다. **문서만 고치고 장면을 안 고치면 그게 어긋남의 시작이다.**
