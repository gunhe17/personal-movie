# s04 바로링크 — 검사받으러 한 번, 결과 들으러 한 번

장면이 약속한 것: `scenes9.html` §04 — 오가는 일이 두 번인 이유는 결과가 늦어서가 아니라 채점이 사람 손에 있어서다.
기법: `faceless.html` §02(3번 줄) — AFTER는 ③ Screenlife, "문자 → 인증 → 수행 → 보고서 → 열람"의 Parisian Love 구조.

**배역: 검사 축.** s01이 접수한 햇살지역아동센터 세 아이 중 하나(윤도현 · 만 12세)에게
같은 케이스의 **스마트폰중독검사**를 집으로 보낸다. 로르샤흐는 s02에서 센터로, 이건 s04에서 집으로 — 한 접수가 두 갈래로 갈린 그 지점이다.

## 흐름 — 웹만 찍는다

폰 파트(응시·PDF 열람)는 이 장면에서 **뺀다**. 촬영은 링크를 만들어 보내는 데까지다.

1. 검사 현황 → 케이스 카드의 `바로링크 전송`
2. 모달 — 보낼 검사(모바일 가능 검사만 뜬다) → 수신자 이름 · 번호
3. `바로링크 전송` → 링크 생성 + 발송 → **전송 내역** 탭으로 넘어간다
4. 전송 내역에 수신인 · 방식 · 상태가 남는다

폰 화면 없이도 장면이 서는 이유: 아날로그의 고통은 "**다시 온다**"였고, 그 반대는 "보냈다"가 아니라
**보낸 것이 남는다**이다. 전송 내역이 그 증거다. 응시·열람은 폰 경로가 정해지면 별도 컷으로 붙인다.

## 코드에서 확인한 것

| | 근거 |
|---|---|
| **바로링크 버튼은 케이스에 온라인 검사가 있어야 뜬다** | `AssessmentCaseCard.svelte:117-121` `showSendLink` · `AssessmentCaseTable.svelte:148` `item.hasOnlineLink`. 베타 숨김 플래그는 둘 다 `false`(`status/constants.ts:18,21`) |
| 온라인 응시 가능 검사는 **18종 중 2종** | `assessments.supports_online` — 스마트폰중독검사(만 10-19세) · 스마트폰 과의존 성인·고령층 척도(만 20세 이상). **s01 아이들의 나이가 여기서 역산됐다**(CAST.md) |
| 보낼 수 있는 검사도 그 2종으로 걸러진다 | `SendResultModal.svelte:149-180` `supportedIds` — `supports_online`만 남긴다 |
| 마감일 선택지 | `SendResultModal.svelte:202-205` — 3일 / 7일 / 15일 / 30일 뒤 |
| 수신자 검증 | `:246-254` — 이름 필수, 전화는 `^01\d{8,9}$` |
| **4자리 인증번호는 서버가 만들어 DB에 둔다** | `assessment_send_links.verification_code` `String(4)` — 문자가 안 가도 여기서 읽을 수 있다 |
| 링크 주소 | `/verify-link?send_link_id=<id>` — 공개 라우트, `ssr=false`. 단계는 `verify → list → exam` |
| 앱 설치가 필요 없다는 말은 사실이다 | `/verify-link`는 `(protected)` 밖의 일반 웹 페이지다 |

## 준비가 필요한 것 · 아직 막힌 것

| | |
|---|---|
| **시작 상태** | s01을 먼저 돌려야 이 장면의 케이스가 생긴다(AC0002~4, 각각 로르샤흐 + 스마트폰중독검사). 검사 축은 s01 → s04 순서로 준비한다 |
| **문자 발송** | **해결됨.** 로컬엔 LGU+ 자격증명이 없어 발송이 항상 실패하고, 그 실패가 `MessageStatus.FAILED`로 굳어 "전송이 실패했어요" 경고로 화면에 나왔다. 발송 경계에 드라이런을 넣어 끊었다 — 아래 참조 |
| **폰 파트** | 이 장면에서 뺀다. 폰은 내담자 앱이 아니라 **모바일 웹**(`/verify-link`)이라 현재 SPEC의 폰 경로(`capture-phone.mjs`, 시뮬레이터 앱 창 전제)로는 못 찍는다. 필요해지면 SPEC을 확장한다(후보: 시뮬레이터 Safari + `simctl openurl`) |
| **웹·폰 동시 촬영** | s04·s05는 두 러너를 동시에 시작해 각 meta의 `captured_at`과 첫 조작으로 정렬한다(capture-service 절차 7) |

## 발송 드라이런 (제품 변경)

`settings.MESSAGING_DRY_RUN`이 참이면 `factory.get_messaging_client()`가 LGU+ 어댑터를 만들지 않고
`DryRunMessagingClient`(`app/infrastructure/messaging/dryrun.py`)를 돌려준다. 반환 모양이 같아서
소비처(`SendMessageService`)는 차이를 모르고, 발송 로그가 `sent`로 남아 **화면·DB·전송 내역이 전부 일관된다**.

UI를 건드리지 않았다는 점이 중요하다 — 실패를 숨긴 것이 아니라, 자격증명이 없는 **환경**을 메운 것이다.
운영 기본값은 `False`이고, 촬영 환경만 켠다.

```bash
MESSAGING_DRY_RUN=true uv run uvicorn app.main:app --host 0.0.0.0 --port 3502
```

## 촬영

| | |
|---|---|
| 조작 | `_scripts/s04-sendlink.mjs` — 목 대본 없음(**에이전트가 나오지 않는 장면**) |
| 시작 | `/assessment/status` · `--state _state/local-saas-counselor1.json` |
| 전제 | **s01을 먼저 돌린다** — 그래야 온라인 검사가 든 케이스가 생기고 버튼이 뜬다 |
| 수신자 | 윤보호(지어낸 보호자) · 01033360118. 관계는 Select이고 기본값 '엄마'라 건드리지 않는다 |
| 마감일 | 건드리지 않는다(선택 사항). 넣으려면 3/7/15/30일 뒤 중에서 |

## 리듬 (SPEC v4)

제품을 기다리는 자리를 고정 `hold`에서 `until(조건)`으로 바꿨다. 전송 뒤 `hold(4000)`으로 뭉개던 것이
`until('text=윤보호')`가 되어 **실제로 걸린 만큼(+0.7초)만** 기다린다 — 전송 내역에 수신인이
`이름 · 번호`로 뜨는 그 순간이다(`SendLinkHistory.svelte:167`).

| | v3 (t01) | v4 (t02) |
|---|---|---|
| 전체 | 27.05초 | **12.67초** |
| 전송 후 대기 | 4.0초 고정 | 0.7초 (until) |
| 끝 빈 화면 | 1.5초 | 0.6초 |

리허설 실측: **10단계 10.1~10.7초 · 서버 오류 0**. DB 확인 — 링크 1건(인증번호 4자리) · 발송 로그 `sent`.

### 셀렉터 함정 둘 (실측)

- **`바로링크 전송`이라는 버튼이 둘이다** — 케이스 카드와 모달 제출. `.first()`는 모달 뒤의 카드를 눌러
  **아무 일도 일어나지 않고 오류도 안 난다**. 모달 제출은 `getByRole('button', {name:'바로링크 전송'}).last()`
- **관계는 `<input>`이 아니라 `<Select>`** — `input[placeholder="관계"]`는 없다
