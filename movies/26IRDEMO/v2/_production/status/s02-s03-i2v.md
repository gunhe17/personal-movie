DONE — S1~S3 AI 컷 11개 전부 영상화 · 절차 표시줄 합성 완성본 CN.N_ai.mp4 배치 · 스토리보드 아티팩트 반영.

# s02-s03-i2v — 2026-09-14

앞 기록: `s01-i2v.md`(S1 네 컷). 이 문서는 S2 · S3 일곱 컷과 **완성본 규격 변경**을 적는다.

## 완성본 규격 — `video.mjs pick`이 무대 컷과 같은 것을 굽는다

`CN.N_ai.mp4` = 채택 테이크를 **cuts.json `length`로 앞에서부터 자르고** · 30fps · 1920×1080 · 무음 ·
cuts.json `steps`의 절차 표시줄(`_assets/steps/SN-흐름/`)을 motion-stage `render.mjs`와 같은 규칙으로 얹는다
(head 0초부터 · hold PNG는 head 뒤에서 tail 앞까지 · tail은 끝에 · mov premultiplied / png straight).
원본 테이크는 `tN/`에 남는다. S1 네 컷도 이 규격으로 다시 구웠다(C1.1은 `steps: null`이라 자르기만).

무대 컷(C1.7~1.9 · C2.4 · C2.5 · C3.6 · C3.7)은 spec에 `steps`가 들어 있어 **표시줄이 이미 구워져 있다** — 손대지 않았다.

## S2 · S3 일곱 컷

전부 `Locked-off tripod shot` + 화면 잠금 절(린트 강제). 모델 3146(Kling 3.0 Pro 1080 무음) · 초당 100 크레딧.

| 컷 | 테이크 | 크레딧 | SSIM 시작↔끝 | 판정 | 걸리는 것 | 완성본 |
|---|---|---|---|---|---|---|
| C2.1 검사진행 | t1 | 400 | 0.88 | 통과 | — | 3s · S2-기존 1 |
| C2.2 반응기록 | t1 | 500 | 0.83 | 통과 | 동작이 작다(검지가 조금 내려감) | 4s · 2 |
| C2.3 보고서작성 | t1 | 500 | 0.88 | 통과 | — | 4s · 3 → out |
| C3.2 문자한통 | **t2** | 400 + 400 | 0.72 | 통과(경미) | **t1 탈락** — 2s에 화면이 켜지고 폰이 두 겹으로 두꺼워짐. t2는 "들지 말고 책상 위에서 끌기 · 한 장짜리 얇은 폰" | 3s · S3-기존 1 |
| C3.3 답이늦다 | t1 | 400 | 0.78 | 통과 | 끝 0.5s에 손이 크게 움직임 — 컷 밖 | 3s · 2 |
| C3.4 전화둘부재중하나 | t1 | 400 | 0.87 | 통과 | 수화기가 프레임 오른쪽 위로 나간다(통화 동작 — 얼굴 없음) | 3s · 3 → out |
| C3.5 다시쓴다 | t1 | 400 | 0.78 | 통과(경미) | 지운 자국 모양이 조금 바뀌고 지우개 가루가 밀림 · 첫 프레임 대비 약간 크롭(0.73) | 3s · 표시줄 없음 |

> **절차 표시줄 재구성 (같은 날 19시 · 사용자 지시)** — S1-기존 4단계(C1.1 전화 문의 포함 · C1.4가 out) ·
> S3-기존 3단계(C3.2 · C3.3 · C3.4만, C3.4가 out, C3.5는 steps null). 옛 판은 `v2/_dropped/steps-S1-기존-3단계` · `steps-S3-기존-5단계`.
> 표시줄이 바뀐 여덟 컷은 원본 테이크 그대로 `pick`만 다시 했다(생성 없음). 위 표의 완성본 열은 재구성 뒤 값이다.

S2·S3 **3,400 크레딧** · S1 포함 누계 **5,200**. generationId · 프롬프트 전문 · 판정은 각 컷 `motion.json`.

## 스킬에 들어간 것

- `pick` → 완성본 규격(위)
- 린트: 탁상전화(desk phone · telephone · handset)는 화면으로 세지 않는다 — C3.4 오탐
- 판정 도구는 아직 스킬 밖(scratchpad `review.sh` — 0.5초 그리드 + SSIM). 반복되면 `video.mjs review`로 올린다

## 스토리보드 아티팩트

AI 카드 11장: 출처를 `CN.N_ai.mp4`로, 길이를 "실측"으로(무대 카드와 같은 표기). 푸터 `AI 영상 11/23`.
S1은 앞 작업에서 C1.1 → C1.3(카톡 컴포넌트) → C1.5 → C1.4로 재구성 · 컷 번호 고정(뺀 번호는 비워 둔다).

## S3 5초 재제작 · S4 두 컷 (같은 날 19:3x~20:2x · 사용자 지시)

**S3 AI 네 컷을 5초 기준으로 다시 생성**했다(cuts.json `length` 3.0s → 5.0s · `gen --duration 5`).

| 컷 | 채택 | 크레딧(누적) | 판정 |
|---|---|---|---|
| C3.2 문자한통 | t3 | 500 | 통과 — 화면 어두움 · 폰 한 장 유지 |
| C3.3 답이늦다 | **t4** | 500×3 | t2 화면 여러 번 켜짐 → t3 1회 번쩍 → **t4 0회**. 처방: 엄지를 화면에서 떼어 옆면에 두고 `the only light in the frame comes from the window` |
| C3.4 전화둘부재중하나 | t3 | 500×2 | t2 본체에 없던 둥근 부품 생성 → t3에 `the telephone base and its keypad stay exactly as they are` 넣어 해소 |
| C3.5 다시쓴다 | t2 | 500 | 통과(경미) — 연필 끝 자리에 짙은 얼룩이 생겨 커진다 |

**5초에서 새로 드러난 것:** 3초에서 안 보이던 실패가 2~3초 구간에 있다(C3.3 화면 점등 · C3.4 소품 변형).
**길이를 늘리면 판정을 다시 해야 한다.**

**S4 두 컷은 서브에이전트가 맡았다**(컷당 1 에이전트 · 프롬프트~판정~합성). 둘 다 t1 채택.

| 컷 | 크레딧 | SSIM 시작↔끝 | 완성본 |
|---|---|---|---|
| C4.1 회기사이두줄 | 400 | 0.84 | 3s · S4-기존 1 |
| C4.2 밤9시40분 | 500 | 0.91 | 4s · S4-기존 2 → out |

에이전트에게 준 제약: 컷 폴더 밖 금지 · `--max-credits` 고정 · 최대 2테이크 · 스킬 스크립트 수정 금지.
결과는 이 세션이 규격(1920×1080 · 30fps · 컷 길이)과 표시줄 합성을 따로 확인했다.

**크레딧 누계**: S1 1,800 + S2·S3 3,400 + S3 5초 재제작 3,000 + S4 900 = **9,100**.

## 남은 것

- S4~S8 AI 컷 12개는 `motion.md`가 없다 — `video.mjs new`부터
- C1.1은 사용자 확정본(모니터 가짜 글자 인지). 새 규칙으로 다시 뽑으면 t2
