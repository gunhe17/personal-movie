# s03-채점보고서 — 서류는 257분

기기: web
배역: **보고서 축 — 박지우(1991-02-14)**. 배터리 3종(로샤·HTP·SCT) 확정본이 한 문서로 묶인다  (정본 CAST.md)

## 조작 순서
종합보고서(박지우, 배터리 3종 확정) → 스크롤 → Ⅰ~Ⅴ 섹션 → 근거가 붙은 문단. 무대는 마인드봄(4503).

## 캡처 지점

리허설 실측(2026-09-10, SPEC v4). 무대는 마인드봄(4503). **테이크 둘을 따로 찍는다** — 하나는 진짜, 하나는 시연.

### A · AI 초안 생성 (`s03_web_draft_t01` · 19.07s)

- 시작: 종합 소견·제언이 빈 보고서, 상태 배지 `초안 작성`
- 종료: 두 섹션이 채워지고 배지가 `AI 초안`, CDSS 푸터
- 노컷 **7.9–14.9** — 빈 칸이 채워진다. 화면이 안 움직이므로 컷 없이 간다

| 구간 | 무엇 |
|---|---|
| 2.4–7.5 | 문서를 훑는다 — 임상가가 쓴 곳 · 검사 결과 3종이 한 문서에 |
| 7.5–10.0 | **빈 칸 둘** (`종합 소견 내용을 입력하세요` / `제언 내용을 입력하세요`) |
| **10.0–14.9 노컷** | 버튼 → 모달 → 서버가 답한다 → 같은 자리에서 두 칸이 채워지고 보라 `AI` 배지 |
| 15.6–17.0 | CDSS 푸터 — 모델명과 책임 소재. ⚠️ 모델명이 `rule-based-fallback`으로 찍힌다 |

### B · AI 종합 리뷰 · 구간 리뷰 (`s03_web_aireview_t01` · 28.78s) — **시연용**

- 시작: A4 편집기, 툴바 우측 `AI 종합 리뷰`
- 종료: 구간 리뷰 수정안이 본문에 반영된 직후

| 구간 | 무엇 |
|---|---|
| **0.7–6.8 노컷** | 네 단계로 문서를 훑는다 → 완성도 55점 · 검토사항 3건 |
| **6.8–11.3 노컷** | `섹션 누락` → `초안 작성` → **바뀔 문장 미리보기** → `본문에 적용` → 지적이 사라지고 점수가 오른다 |
| 11.3–15.4 | `서술 모순` — 중등도 vs 경미. 양방향 통일 |
| **15.6–26.0 노컷** | 문장을 마우스로 긋는다 → `AI 분석` → 5건(맞춤법·표현·근거 연결) → `수정 후 미리보기` → 모두 적용 |

## 리허설

```bash
# A
_scripts/s03-reset.sh
node .claude/skills/scene-prep/scripts/rehearse.mjs --scene s03-채점보고서 \
  --url "http://localhost:4503/clients/<clientId>/reports/<reportId>" \
  --state _state/local-mindbom-counselor1.json --script _scripts/s03-draft.mjs

# B (되돌릴 것 없음 — 저장이 없다)
node .claude/skills/scene-prep/scripts/rehearse.mjs --scene s03-채점보고서 \
  --url "http://localhost:4503/examinations/<examId>/report?ids=<3종>" \
  --state _state/local-mindbom-counselor1.json --script _scripts/s03-review.mjs
```

마지막 결과: **A 24단계 18.4초 · B 27단계 27.4초 · 둘 다 서버 오류 0** (2026-09-10).
A는 DB 확인 — `status=ai_generated` · `ai_model_version` 기록 · 빈 섹션 0.

## 테이크

| 테이크 | 길이 | 무엇 |
|---|---|---|
| `report_t01` | — | 완성된 보고서를 스크롤만. **AI가 일하는 장면이 없어** 폐기 |
| `report_t02` | — | 같은 구성. 폐기 |
| **`draft_t01`** | **19.07s** | **AI 초안 생성 — 전부 진짜.** 빈 칸 둘이 눈앞에서 채워진다 |
| **`aireview_t01`** | **28.78s** | **AI 종합 리뷰 + 구간 리뷰 — 시연용.** features.md의 경고를 읽고 쓴다 |
