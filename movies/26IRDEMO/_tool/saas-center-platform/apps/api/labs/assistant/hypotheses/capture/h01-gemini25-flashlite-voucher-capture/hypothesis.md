---
name: h07-gemini25-flashlite-voucher-capture
status: open
model: google/gemini-2.5-flash-lite
opened: 2026-07-28
closed: -
lab: e1/lab.py
results: e1/results.jsonl
decision: -
derived_from: - (바우처-정규화-토의.md 추출 구조 제안 + 모델 후보 조사의 집행)
---
# gemini-2.5-flash-lite는 바우처 문서 캡처(서술 단위 충실 추출)에서 채택 문턱을 넘는다

h06과 같은 **측정형** — 행동 기전이 아니라 후보 모델의 과제 적합성 실측. 채택 문턱 =
① 값 날조(원문에 없는 수치) 0 ② quote 원문 일치(S2c 스내핑 전제) ③ 명시 부재 시 기권
(null — 전국·유사값 추정 금지) ④ 골든 값 정확. 근거 요구 특성은 레포 루트
`바우처-정규화-토의.md`(추출 구조 개선 제안 — 캡처는 "있는 것만, 없으면 null")가 정의.

## 조사

- [knowledge/gemini-2.5-flash-lite/](../../knowledge/gemini-2.5-flash-lite/) — 본 가설과 동시 등재
  (E1 실측이 초기 내용). 외부: Vectara HHEM 환각률 3.3%·응답률 99.5%(영어 요약 프록시),
  ExtractBench(PDF→JSON은 프론티어도 난제 — 필드 통과 4.6%).
- 계보: gemini-3.1-flash-lite responder 채택(D5) — 같은 계열의 형식 준수 실적.
  h04(프롬프트 저항)는 행동 성향 가설이라 측정형인 본 건에 직접 적용 없음. 단 s5·s7
  실패에 대한 처방이 "프롬프트 문구"라면 h04 게이트(페어드 1회 상한) 적용 대상.
- 비용: flash-lite는 후보군 최저가 티어 — E1 실측 16시행 $0.0065.

## 실험 E1 [탐색] — 원문 슬라이스 8문항 × K=2, 결정적 코드 채점

### 설계

- 문항 = `fixtures.json` — first-collect 원문 슬라이스 8개(발달재활 개요·심리지원 욕구·
  마음투자 대상/가격·경기 추진지역표·누구나돌봄·기권 2문항). 골든 = 토의 문서 §1~§10
  실측값. **기권 문항 2개 포함**(정보 부재 축 요청 → null 기대)이 이 가설의 핵심 축.
- 판정 = lab.py 결정적 채점기: 골든 대조 · quote 전건 원문 substring · 환각 수치
  검출(응답 내 모든 수치>10의 원문 실재) · 기권 null 정확. temp 0, json_object 모드.
- 레인 = 탐색(사전 등록 없음, 소규모 N) — 증명력 0, E2 확증 설계 재료.

### 결과 (2026-07-28, 16시행, $0.0065)

```
값정확 44/60(73%) · quote 122/127(96%) · 환각 수치 0건 · K간 판정 불안정 0문항 · wall 0.6~4.3s
```

실패 8종 전건 원시 응답 분류(분류가 이 라운드의 산출물):

| 실패 | 분류 | 실체 |
|---|---|---|
| s1 단가·등급표합계·한도 (3) | **골든 결함** | 모델이 옳음 — 슬라이스 실값 26만/32,500원(quote 일치·환각 0), 골든은 v2 voucher.json 값(25만/3만). **v2 추출 부정확의 추가 증거** — 골든은 원문 기준으로 재작성 필요 |
| s1 연령_최대 (1) | **설계 검증** | "18세 미만"→최대 18 반환(환산 미적용). 캡처는 충실 — **환산은 코드 소관이라는 2단 구조 제안을 실측이 지지** |
| s4 추진_수 (1) | 프롬프트 과소지정+표 정렬 난제 | ○ 선별 지시 없음 → 창 내 시군명 전수 31행 나열. 깨진 OCR 표의 ○/X 정렬은 검산(헤더 N)으로 잡을 문제 |
| s5 소득_없음·구간 (2) | **축 경계 미탑재** | "소득 관계없이" 문서에서 자부담 차등 구간을 소득기준으로 오배치 — §3 진입/차등 분리 규칙을 프롬프트에 안 실은 결과. 흡수 오분류(P1)의 미니 재현 |
| s7 지역_null (1) | **기권 실패 — 핵심 실측** | 지역 없는 문서에서 행정 절차 언급("시군구청 지정")을 추진지역으로 강제 배치. 날조는 없으나 요청 축을 채우는 압력에 굴복. s8(명백 부재)은 2/2 통과 — **기권은 "유사물 존재" 조건에서만 깨짐** |

quote 불일치 5건 = 전건 재구성(내용은 원문 유래 확인 — `<br>`·표 셀로 형식 재조립,
외부 지식 주입 0). S2c 스내핑에서 스냅 실패 소견으로 자연 검출될 부류.

### 판정 [탐색]

**충실성 코어(날조 0·quote 96%·외부 지식 0·안정성)는 채택 방향을 강하게 지지.**
실패의 다수는 실험 결함(골든 3)·설계 미탑재(프롬프트에 축 경계·거절 채널 부재)이며,
모델 고유 리스크는 **유사물 존재 시 축 강제 배치** 하나로 좁혀짐 — 처방은 추출 구조
제안에 이미 있음(항목 거절 채널 + 검산). 실측 사실은 knowledge/ 3파일로 승격 완료.

## 다음 — E2 [확증] 설계 방향

① 골든 교정(원문 기준 재작성) ② 캡처 프롬프트에 거절 채널("어느 축에도 확신 없으면
`항목`으로")·축 경계 규칙(진입/차등 분리·○ 선별) 동봉 ③ haiku-4-5와 페어드 ④ 판정
v2 사전 등록(N·K·MDE 산출 후 커밋). s7형 기권 실패의 잔존 여부가 승격/재평가 분기점.

## 종료 판정

(진행 중 — status: open. E1 탐색 1라운드 완료)
