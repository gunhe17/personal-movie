---
model: x-ai/grok-stt-1.0
topic: 운영
updated: 2026-07-28
---
# 운영 — 상태·비용·채택 조건

- **상태: 미채택·미배선 — 자체 실측 0.** 이 폴더는 후보 등재(벤더 문서·발표 전사)이며, 용도가
  생기면 아래 체크리스트 실측 후 특성.md의 벤더 수치를 실측으로 교체한다.
- 계보: xAI 직결 GA 2026-04-18 → OpenRouter 등재 2026-07-23. 기능 개선 활발(5월 `smart_turn`,
  7월 `vad_threshold` — [릴리즈 노트](https://docs.x.ai/developers/release-notes)). 자매 API로
  TTS($4.20/M자)·Speech-to-Speech 존재.
- 가격: **배치 $0.10/audio-hour · 스트리밍 $0.20/audio-hour**(xAI 직결). OpenRouter 경유는
  배치만 $0.10 — 턴별 실비는 응답 `usage.cost`로 확인 가능.
- provider가 xAI 단독 — laguna와 같은 단일 업스트림 구조라 **429 파도 리스크 동형**(운영 전 실측 필요).

## 채택 전 실측 체크리스트 — 집행: [h06 E1](../../hypotheses/h06-grok-stt-korean-meeting/hypothesis.md)

1. **한국어 정확도** — 상담 도메인 어휘·이름(내담자명). 벤더 벤치마크에 한국어 수치 없음(특성.md).
   편향 레버 `keyterm`은 직결 전용.
2. ~~OpenRouter 경유 시 word timestamps·diarize가 통하는가~~ → **실측 완료(h06 E1)**:
   verbose_json word timestamps 통과 / diarize 조용히 무시(계약.md).
3. 길이 한계 — **실측 완료(h06 E1)**: 60초 초과 조용 절삭은 **직결에서도 동일**(62.6s 실측)
   → 긴 녹음은 양 경로 모두 부적합, 우회는 60초 청킹뿐.
4. 429·지연 분포 — 벤더·외부 모두 지연 실측치 미공개.
