# 리빙랩 운영 세팅 체크리스트 (W4)

> 2026-07-20 작성. 규모 도입 감사·24항목 워크스루에서 실증된 함정 기반. 배포 담당자가 순서대로 실행한다.

## 1. 배포 환경

- [ ] 웹 배포에 **`PUBLIC_APP_ENV=livinglab`** 환경변수 설정 (미설정 시 hostname 폴백 — app.mindscope.kr 외 호스트는 development 취급되어 **구독이 노출됨**)
- [ ] 배포 후 **`GET /healthz/app-env`** 응답 확인: `{"appEnv":"livinglab","showSubscription":false,"showAiFeatures":true}`
- [ ] AI 기능 확인: 사이드바에 에이전트·필드노트 노출, `/subscription` 접근 시 대시보드 리다이렉트

## 2. DB 준비 (순서 중요)

- [ ] `uv run alembic upgrade head` — **`stamp head`로 만든 환경은 데이터 백필이 스킵되므로 금지** (규모감사 F5: 신규 센터 바우처 전면 403의 원인)
- [ ] `uv run python -m scripts.seed.common` — 역할·권한 카탈로그 + **검사 18종** + 바우처 카탈로그 (멱등)
- [ ] **권한 카탈로그 검증** (F5 가드):
  ```sql
  -- 56개, read/write/delete:voucher 3종 포함이어야 함
  select count(*) from permissions;
  select code from permissions where code like '%voucher%';
  ```
  코드 기준: `apps/api/app/core/permissions.py` enum ↔ permissions 테이블 diff 0이어야 함
- [ ] 검사 18종 확인: `select count(*) from assessments where deleted_at is null;` → 18

## 3. 평가 센터·계정 세팅 (검증된 절차)

1. [ ] 평가자별 계정 가입 (`/signup`)
2. [ ] 대표 평가 센터 신청(`POST /api/v1/centers/applications/`) → 플랫폼 관리자 승인
   - 승인 계정: 플랫폼 관리자(super admin) — 시드 기준 `imomtae@insighter.co.kr`
3. [ ] **센터 취급 검사 연결** — 신규 센터는 승인 시점의 public 검사만 자동 연결됨. 18종 전체 연결:
   `POST /centers/{cid}/center-assessments/{assessment_id}` (매니저 토큰, 워크스루 setup-assessments 스크립트 참조)
4. [ ] 구성원(전문가) 초대 → 수락 (역할 옵션명: **전문가**/매니저/스태프)
5. [ ] 상담실·프로그램 등록 (세팅 체크리스트 카드가 캘린더에서 안내 — 3단계)
6. [ ] 복수 센터 평가자: 2호 센터 신청→승인으로 다중센터 전환 검증 가능 상태로
7. [ ] 취급 바우처 등록(설정>바우처 관리, 카탈로그 선택) + 내담자 바우처 발급
   - 임박 알림 확인용: 유효 종료일을 **평가 주간 +7일 이내**로 1건
8. [ ] 전문가 자격·학력·경력 샘플 1건 등록 (내 정보) — 인증 뱃지 프로세스는 별도 확인
9. [ ] 내담자 검사용 계정 세팅 — **검증된 절차(07-20)**: ① 일반 가입으로 검사 계정 생성 ② 센터 구성원 초대 API로 STAFF 합류(`role_code` enum엔 STAFF 없음 → UI '스태프' 초대 또는 초대 후 역할 변경) ③ **STAFF에 `write:assessment_case` 권한 필요**(기본 없음 → 제출 403. **W-7 확정(07-21): 평가 센터의 STAFF 역할에 센터-로컬로 추가** — 전역 기본 권한 변경은 8월 2차 보완에서 별도 결정) ④ 검사 로그인(/assessment/login) → 센터 자동선택 → 내담자 이름·생년월일 2단 확인 → 수행
   - ✅ 문자 속 검사 링크(`/verify-link`) **구현 완료(07-21, W-6)** — 인증번호 4자리 → 계정 없이 검사 수행·자동 채점까지. 실기기 리허설에서 문자 실수신 경로 재확인

## 4. 알려진 제한 고지문 반영 (평가 양식 붙임)

- 웹 스케줄 통계: 준비 중 (모바일 일정 탭 기준 평가)
- AI 검사 해석 보조(D2)·바우처 서류 자동 생성(D3): 이번 차수 연기
- 바우처 알림·납부 증빙(D4): 상태 알림으로 제공
- AI 기능(에이전트·일지 초안·필드노트): 리빙랩 환경 활성, 크레딧 확인 후 평가 시작
- 결과 다운로드 링크 1시간 유효, 웹은 브라우저 푸시 없음(화면 내 알림)

## 5. 리허설 (실기기)

- [x] ~~검사 end-to-end 전반부~~ **완료(07-22 실기기)**: 접수 → 바로링크 발송 → 알림톡 거절(21044) 시 **문자 자동 대체발송** → 휴대폰 실수신 → /verify-link 원터치 진입 → 인증번호 → 검사 진입까지 스테이징 실증(로그 교차 확인). 잔여: 수행·제출→자동 채점→결과 전송→보고서 다운로드 실기기 확인
  - ✅ 문자 본문 링크·인증번호 포함 확인(W-8). 알림톡은 미등록 상태지만 **SMS 자동 대체발송 폴백 배포(07-22)** — 카카오 템플릿(SEND_LINK_01) 정식 등록은 운영 절차로 진행(등록되면 알림톡 자동 복귀)
- [ ] 모바일: 푸시 알림(제출·일정), 필드노트 녹음 안정성(앱 이탈 시나리오 기록)
- [ ] **모바일 릴리즈 빌드 확인** (dev 전용 요소가 안 새는지 — 07-21 수정분 포함):
  - 실험실 탭 숨김(`__DEV__`), 내 센터 "내 워크스페이스" 카드 숨김(M-5)
  - 홈 "오늘 살펴볼 것들" = 실신호만(횡단 신호 §4 + 다음 일정 prep-signals, mock 없음 — M-4)
  - 동시간대 일정 카드 레인 분할(M-2), 내담자 탭 등록 버튼(M-9), 검사 청구 단가 입력(M-8)

## 진행 현황 (로컬 리허설 환경 = 새봄심리상담센터)

- ✅ 검사 18종 시드 + 새봄센터 18종 연결 (07-20)
- ✅ 취급 바우처(우리아이심리지원서비스) + 발급 2건(서지우 8/10 임박·강민준 12/31) — 대시보드 "새 바우처" 신호 확인
- ✅ 2호 센터(새봄아동발달센터) 승인 — 박서봄 다중센터 전환 가능 상태
- ✅ 자격 샘플 1건(이하늘, 임상심리사 1급)
- ⬜ 내담자 검사 계정, 실기기 리허설, 평가자 5명 실계정(리빙랩 배포 환경에서)
