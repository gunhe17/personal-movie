apiVersion: apps/v1
kind: Deployment
metadata:
  name: mindscope-admin
  namespace: mindscope-dev
  labels:
    app: mindscope-admin
    environment: dev
spec:
  replicas: 1
  selector:
    matchLabels:
      app: mindscope-admin
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  template:
    metadata:
      labels:
        app: mindscope-admin
        environment: dev
    spec:
      containers:
        - name: admin
          image: 786180311928.dkr.ecr.ap-northeast-2.amazonaws.com/mindscope-prod/admin:latest
          imagePullPolicy: Always
          ports:
            - containerPort: 3001
          envFrom:
            - configMapRef:
                name: mindscope-admin-config
          livenessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 15
            periodSeconds: 10
            failureThreshold: 3
          readinessProbe:
            httpGet:
              path: /healthz
              port: 3001
            initialDelaySeconds: 5
            periodSeconds: 5
            failureThreshold: 3
          resources:
            requests:
              cpu: "100m"
              memory: "128Mi"
            limits:
              cpu: "500m"
              memory: "512Mi"
