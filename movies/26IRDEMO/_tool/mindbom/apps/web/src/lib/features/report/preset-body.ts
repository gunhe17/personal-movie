/**
 * 종합보고서 본문 초안 (시연용 프리셋).
 *
 * ⚠️ 시연용: 실검사 3종(로르샤하·HTP·SCT)에 맞춘 예시 초안이다.
 *    실기능 전환 시 POST /api/report/summarize 응답으로 대체한다.
 *
 * 대상은 **윤도현(2014-05-08, 만 12세, 남)** — 영상의 검사 축 주인공이다.
 * s01에서 햇살지역아동센터 단체 검사로 접수되고, s02에서 로르샤하를 실시하고,
 * 여기 s03에서 그 셋이 한 보고서로 묶인다. 2026-09-10에 박지우(만 35세)에서
 * 옮겼다 — SCT 문항이 아동·청소년용("여자애들은", "나의 학교생활은")이라
 * 성인 대상이 애초에 어긋나 있었다.
 *
 * 본문 안에 데모용으로 심어 둔 것들이 있다(각 문단 주석 참고) — 종합 리뷰가
 * 지적을 실제로 잡아내는 걸 보여주기 위한 장치라, 지우면 리뷰 시연이 빈손이 된다.
 *   · Ⅲ. 행동 관찰을 비워 둠      → "섹션 누락" 지적
 *   · Ⅳ-5)에 '매우'·'되어진'·'환자' → 표현 교정 제안
 *   · Ⅳ-2) '중등도' vs Ⅴ '경미한'  → "문서 내 모순" 지적
 */
import type { InitialBlock } from '$lib/components/document-editor/components/PaginatedEditor.svelte'

// ── 본문 프리셋 ──
// ⚠️ 시연용: 실검사 3종(로르샤하·HTP·SCT)에 맞춘 예시 초안.
//    실기능 전환 시 POST /api/report/summarize 응답으로 대체.
export function presetBody(): InitialBlock[] {
  const h = (text: string): InitialBlock => ({
    kind: 'body',
    text,
    blockStyle: 'heading'
  })
  const p = (text: string): InitialBlock => ({ kind: 'body', text })
  const li = (text: string): InitialBlock => ({
    kind: 'body',
    text,
    listType: 'bullet'
  })

  return [
    h('Ⅰ. 의뢰 사유 및 배경'),
    p(
      '햇살지역아동센터의 의뢰로 실시된 단체 심리평가 대상 아동이다. 센터 교사는 또래와 어울리는 데는 무리가 없으나 자기 이야기를 거의 하지 않는다고 보고하였고, 보호자는 집에서 말수가 줄고 힘든 일이 있어도 내색하지 않는다고 보고하였다. 현재의 정서 상태와 가정 내 소통 양상, 대처 자원을 확인하기 위해 종합 심리평가가 의뢰되었다.'
    ),
    p(''),

    h('Ⅱ. 실시한 검사'),
    // 실시 방식을 한 구절씩 붙여 둔다. 이름만 세 줄이면 53자라
    // 종합 리뷰의 "섹션 서술이 짧습니다"(60자 미만, overall-review.ts:205)에 걸려
    // 불릿 목록에 대고 서술이 짧다고 지적하는 어색한 그림이 된다.
    // 날짜는 넣지 않는다 — 재시드하면 검사 시각이 지금 기준으로 다시 맞춰져
    // 사이드바 표시와 어긋난다(seed.py 주석).
    li('Rorschach (로르샤하 잉크반점검사) — 투사법, 개별 대면 실시'),
    li('HTP (집-나무-사람 그림검사) — 투사법, 그림 4장 개별 실시'),
    li('SCT (문장완성검사) — 투사법, 40문항 자기보고'),
    p(''),

    h('Ⅲ. 행동 관찰'),
    // ⚠️ 데모용: 종합 리뷰가 "섹션 누락"을 지적하도록 의도적으로 비워 둔다.
    //    (임상가가 아직 작성하지 않은 상태를 재현)
    p(''),

    h('Ⅳ. 검사 결과'),
    p(
      '1) 반응 양상 — 로르샤하에서 총 22개의 반응이 산출되어 해석 가능한 프로토콜이 확보되었다. 반응 수가 충분하고 카드별 분포도 고르게 나타나 검사 태도에 따른 축소는 관찰되지 않는다.'
    ),
    p(''),
    p(
      // ⚠️ 데모용: Ⅴ의 '경미한'과 짝을 이루는 모순 재료.
      //    구성개념은 overall-review.ts의 CONCEPTS가 아는 것이어야 잡힌다(/우울/).
      '2) 정서 상태 — SCT 정서·대처 영역(30/48)에서 중등도 수준의 우울이 시사된다. “애쓴 걸 아무도 몰라줄 때”, “계속 이대로 아무것도 안 바뀌는 것”과 같은 응답이 반복되어, 정서적 고통이 일시적 반응에 그치지 않을 가능성을 시사한다. 로르샤하에서도 손상 내용 반응이 관찰되어 같은 방향을 가리킨다.'
    ),
    p(
      '3) 가족 관계 — SCT 가족 관계 영역이 36/54로 다섯 영역 중 가장 높다. “서로 조심하느라 할 말을 못 한다”, “같이 살지만 각자 따로 지낸다”는 응답에서 가정 내 정서적 소통이 제한되어 있음이 드러난다. 반면 또래·사회 관계는 16/48로 가장 낮아, 어려움이 관계 전반이 아니라 가정 맥락에 집중되어 있음을 시사한다.'
    ),
    p(
      '4) 자기개념 — SCT 자기 인식 영역(21/48)과 HTP의 위축된 자기상이 함께 관찰된다. HTP 인물상은 크기가 작고 나무 기둥이 가늘게 그려져 자아강도의 취약성이 시사되며, 집 그림에서 문이 작고 길이 생략되어 접촉을 먼저 열지 않는 양상과도 맥을 같이한다.'
    ),
    // 그림 블록은 두지 않는다 — 예전 프리셋은 `/mock-reports/tci/temperament.png`를
    // 박아 뒀는데 그 정적 파일은 목업 표준화 검사 소속이라 함께 걷어냈다(2026-09-10).
    // HTP 실제 그림 네 장은 사이드바 '검사자료'에 실자료로 서므로, 본문에 넣으려면
    // 촬영 중 거기서 눌러 첨부한다. 없는 경로를 박으면 화면에 깨진 이미지가 뜬다.
    p(''),
    p(
      // ⚠️ 데모용 문단 — AI 리뷰 교정 제안을 보여주기 위해 의도적으로
      //    '매우'(주관적 강조어) / '되어진'(이중 피동) / '환자'(용어)를 포함한다.
      '5) 대인 지각 — 로르샤하의 인간운동반응과 협응반응이 확인되어 관계에 대한 기대 수준은 매우 높다. 환자의 경우 관계를 피한다기보다, 먼저 다가가지 않는 방식으로 거리를 두는 것으로 해석되어진 것으로 판단된다.'
    ),
    p(''),

    h('Ⅴ. 요약 및 제언'),
    p(
      // ⚠️ 데모용: 종합 리뷰가 "문서 내 모순"을 잡아내도록, 앞의 Ⅳ-2)에서
      //    '중등도'로 기술한 정서적 부담을 여기서는 '경미한'으로 서술해 둔다.
      '종합하면, 본 아동은 경미한 수준의 우울과 함께 낮은 자기가치감이 세 검사에서 일관되게 확인된다. 가정 내 소통의 제한이 정서를 표현하기보다 혼자 감내하는 대처를 지속시키는 요인으로 보이며, 또래 관계가 비교적 유지되고 있다는 점은 개입의 자원으로 활용할 수 있다.'
    ),
    li('정서 인식과 표현을 목표로 한 개인 상담을 주 1회 권고한다.'),
    li('보호자 상담을 병행하여 가정 내 소통 방식을 다루는 것이 우선 과제로 보인다.'),
    li('또래 관계의 강점을 활용한 소집단 활동 참여를 고려할 수 있다.'),
    li('6개월 후 재평가를 통해 정서 조절 자원의 변화를 확인할 것을 제안한다.'),
    p(''),
    p(
      '본 결과는 심리평가의 보조 자료로서, 임상적 판단 및 다른 정보와 종합하여 해석되어야 한다.'
    )
  ]
}
